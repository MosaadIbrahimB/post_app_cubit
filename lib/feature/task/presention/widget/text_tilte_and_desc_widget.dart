import 'package:flutter/material.dart';
class TextTitleAndDescWidget extends StatelessWidget {
  const TextTitleAndDescWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
